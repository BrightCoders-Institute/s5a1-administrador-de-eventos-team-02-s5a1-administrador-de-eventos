<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to "Team #2", root_path, class: "navbar-brand" %>
    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarText"
      aria-controls="navbarText"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to "Eventos", events_path, class: "btn btn-link" %>
        </li>
        <li class="nav-item active">
          <%= button_to 'Cerrar sesión', destroy_user_session_path, method:
          :delete, class:"btn btn-link" %>
        </li>
         <li class="nav-item active">
          <%= link_to "Mi cuenta", edit_user_registration_path, class: "btn btn-link" %>
        </li>
        <% else %>
        <li class="nav-item active">
          <%= link_to 'Iniciar sesión', new_user_session_path %>
        </li>
        <li class="nav-item active">
          <%= link_to 'Registrarse', new_user_registration_path %>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
