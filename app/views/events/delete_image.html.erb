<h1>Eliminar Imagen del Evento</h1>
<p>¿Estás seguro de que deseas eliminar la imagen del evento?</p>
<%= link_to 'Eliminar', delete_image_event_path(@evento), method: :delete, data: { confirm: '¿Estás seguro?' }, class: 'btn btn-danger' %>
<%= link_to 'Cancelar', event_path(@evento), class: 'btn btn-secondary' %>
